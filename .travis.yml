language: python
sudo: required

before_install:
  - pip install -U pip
  - pip install -U setuptools

script:
  - pip install -r requirements.txt
	-	python -m pytest -v 

deploy:
  provider: pages
  github_token: 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
  keep_history: true
  on:
    branch: main
 
 